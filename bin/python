#!/usr/bin/env bash
set -euo pipefail

ROOT="$(
  cd "$(dirname "${BASH_SOURCE[0]}")/.." &&
  pwd
)"

PYTHON="$ROOT/.venv/bin/python"

test -x "$PYTHON" || {
  echo "ERROR: Legal Kural virtual environment is missing."
  echo "Run: ./bin/bootstrap"
  exit 1
}

exec "$PYTHON" "$@"
