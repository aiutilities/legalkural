#!/usr/bin/env bash
set -euo pipefail

ROOT="$(
    cd "$(dirname "${BASH_SOURCE[0]}")/.." &&
    pwd
)"

export PYTHONPATH="$ROOT/engine${PYTHONPATH:+:$PYTHONPATH}"

exec "$ROOT/bin/python" -m aidpl.manual_task_cli "$@"
