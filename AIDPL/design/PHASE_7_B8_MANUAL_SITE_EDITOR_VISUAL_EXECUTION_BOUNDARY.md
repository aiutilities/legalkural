# Phase 7 B8 — Manual Site Editor Visual Execution Boundary

## Status

`EXACT BOUNDARY APPROVED — LIVE EXECUTION NOT STARTED`

Founder approval records this exact boundary. A later explicit execution gate
is required before entering the WordPress.com Site Editor or changing live
Global Styles.

## Bound checkpoint and live lineage

- Repository checkpoint:
  `934520a3aa175d8bffff02028cc127349ca87b42`.
- TITLE_ONLY remediation checkpoint:
  `67177f1ee8102ddfeea9ff2ae843a3c8861373c2`.
- B8 structural plan SHA-256:
  `7609f095e240c35bb0e66b15fc31a829444982ed7d1522cd2c81fabfb606b8f6`.
- B8 normalized verification SHA-256:
  `aa87497238027bb178ace0621e6cf13a0f295175040581ce47172533ceff7395`.
- Site: `lkaidpl.wordpress.com` (pilot only).
- Active theme lineage: Assembler; theme replacement is excluded.
- Certified article: WordPress post ID `10`; content, title, slug, status and
  publication identity are protected.

## Purpose

Apply the approved contemporary legal-tech presentation through the manual
WordPress.com Site Editor while Coming Soon remains active. This boundary is
limited to visual Global Styles controls already exposed by the active
Assembler theme. It does not permit content, structure, publication or launch
changes.

## Exact allowed visual controls

Only the following values may be entered manually. If a named control is not
available, stop and record the limitation; do not substitute another value.

### Colours

| Control | Approved value | Use |
| --- | --- | --- |
| Background | `#FFFFFF` | Primary canvas |
| Text | `#17212B` | Body text |
| Heading | `#0B1F3A` | Titles and headings |
| Link/accent | `#066872` | Accessible links and active states |
| Primary accent | `#087E8B` | Large accents and rules only |
| Border | `#D9E2EC` | Dividers and borders |
| Alternate background | `#F5F9FB` | Quiet section background |
| Information background | `#DDF3F4` | Restrained information panel |

Teal `#087E8B` must not be used for small body text. Gradients, shadows and
decorative cultural motifs are excluded.

### Typography

| Control | Approved value |
| --- | --- |
| Heading family | `Georgia`, serif |
| Body/interface family | System sans-serif / WordPress system font |
| Base body size | At least `16px`; use `18px` when an exact numeric control exists |
| Body line height | `1.6` when an exact numeric control exists |
| Heading weight | `600` or the nearest named semibold setting |

No font upload, network font, custom CSS or paid-plan upgrade is authorized.
If Georgia or the system family is not exposed by the current plan, retain the
existing family and stop the typography portion for later evidence review.

### Layout and spacing

| Control | Approved value |
| --- | --- |
| Content width | `720px`, if an exact width control exists |
| Wide width | `1200px`, if an exact width control exists |
| Paragraph spacing | Theme default unless an exact global control exists |
| Button shape | Restrained; maximum `4px` radius if exposed |

Do not edit page blocks, template markup, navigation entities, header/footer
content or post content to approximate unavailable style controls.

## Before-state evidence

Before changing a value:

1. Verify `HEAD` and `origin/main` equal the bound checkpoint.
2. Confirm Coming Soon is active and indexing remains blocked.
3. Capture authenticated screenshots of Global Styles: Colours, Typography,
   Layout and any affected Blocks panel.
4. Capture authenticated previews of Home, About, Judgments, Journal, Privacy,
   Disclaimer and post ID `10` at desktop and mobile widths.
5. Record every existing value that will be changed.
6. Confirm post ID `10` retains the certified content and title hashes recorded
   in the B8 closeout.

Screenshots and redacted values must contain no credentials, tokens or private
session data.

## Manual mutation sequence

Perform one saved checkpoint at a time:

1. Open Site Editor → Styles without changing the active theme.
2. Apply the approved global background, text, heading and link colours.
3. Apply approved border and quiet-background colours only where the Global
   Styles interface exposes the corresponding global or section controls.
4. Apply the approved heading and body typography where available.
5. Apply approved content/wide widths where available.
6. Preview Home and post ID `10`; do not edit their blocks or content.
7. Preview the remaining structural pages and header/footer.
8. Save only after the preview passes the stop conditions below.
9. Capture after-state screenshots and the final displayed Global Styles
   values.

This sequence authorizes no logo upload, favicon change, media upload or site
icon adoption. Those require an approved final asset and a separate boundary.

## Validation after save

- Header and footer remain readable on white/navy contexts.
- All normal text meets the approved contrast baseline.
- Keyboard focus remains visible.
- Mobile pages have no horizontal overflow or clipped navigation.
- Heading order and page content are unchanged.
- Post ID `10` content, title, slug, status and publication identity remain
  unchanged.
- Coming Soon remains active.
- Indexing remains blocked.
- No theme, navigation, template, page or post entity is changed.

## Reverse-order rollback

If validation fails, restore the recorded before-state values in reverse order:

1. Layout widths.
2. Typography.
3. Section/border colours.
4. Link, heading, text and background colours.

After rollback, recheck Home and post ID `10`, confirm Coming Soon/indexing are
unchanged, and retain failure evidence. Do not attempt an alternative design in
the same operation.

## Immediate stop conditions

Stop without saving, or roll back the last saved checkpoint, if:

- the active theme is not Assembler;
- a required before-state value or screenshot is missing;
- the interface requests a plan upgrade, custom CSS, font upload or theme
  replacement;
- an action would alter templates, navigation, page/post blocks or content;
- post ID `10` differs from its certified identity or hashes;
- contrast, focus, responsive layout or readability regresses;
- Coming Soon or indexing changes;
- any requested control is unavailable or ambiguous;
- credentials, tokens or private information appear in evidence.

## Hard exclusions

- Theme replacement or activation.
- Global Styles API mutation or scripted WordPress mutation.
- Page, post, template, template-part, navigation or settings mutation.
- Post ID `10` mutation.
- Logo, favicon, site icon or media upload.
- Tamil rendering or literary Kural body presentation.
- Coming Soon removal.
- Search-indexing activation.
- Public launch.

## Completion evidence

The manual operation is complete only when before/after screenshots, recorded
values, rollback readiness, protected post verification and a zero-launch-state
check are reviewed. Repository evidence and any commit require separate exact
boundary approval. B11 remains the sole gate for Coming Soon removal and public
launch.

## Manual visual execution result — 2026-08-24

- Authorization checkpoint: `4a69056`.
- Execution mode: Founder-operated WordPress.com Site Editor in Safari, with guided visual verification.
- Result: `EXECUTED_NO_MUTATION_BLOCKED_BY_PLAN_LIMITATIONS`.
- Active theme typography exposed Inter and a limited bundled font list; Georgia was unavailable. No substitute font was selected.
- Exact custom heading and link HEX controls were inspectable, but applying custom HEX values invoked the WordPress.com premium-styles gate.
- Exact content-width and wide-width controls were inspectable, but applying `720px` and `1200px` invoked the WordPress.com premium-styles gate.
- All staged custom-style changes were abandoned without saving. No upgrade, premium-style removal, or public launch action was taken.
- Certified post ID 10, pages, templates, template parts, navigation, patterns, identity, media, Coming Soon, indexing and launch status were not modified.
- `tamil_rendered` remains `false`; `thirukkural_algorithm_usage` remains `TITLE_ONLY`.
- Further live visual mutation requires a separately approved boundary and, where applicable, an explicit commercial-plan decision.
